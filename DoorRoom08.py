import pygame
import params, Block, imports, Activator

NowLocation = imports.room8_scaled
NowLocationRect = imports.room8_scaled_rect

walls = [

]

activator = [
    Activator.Activator(50, 50, 500, 500)
]