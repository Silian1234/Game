import pygame
import params, Block, imports, Activator, NPS, dialogs

NowLocation = imports.underground_scaled
NowLocationRect = imports.underground_scaled_rect

walls = [

]

activator = [
    Activator.Activator(383, 163, 846, 262)
]

NPSSS = [

]

dialog_window = [

]

newXPos = params.WIDTH / (params.WIDTH / 1777)
newYPos = params.HEIGHT / (params.HEIGHT / 870)