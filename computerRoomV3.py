import pygame
import params, Block, imports, Activator, NPS, dialogs

NowLocation = imports.computerRoomV3_scaled
NowLocationRect = imports.computerRoomV3_scaled_rect

walls = [

]

activator = [
    Activator.Activator(226, 56, 520, 300)
]

NPSSS = [

]

dialog_window = [

]

newXPos = params.WIDTH / (params.WIDTH / 1644)
newYPos = params.HEIGHT / (params.HEIGHT / 324)