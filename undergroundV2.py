import pygame
import params, Block, imports, Activator, NPS, dialogs

NowLocation = imports.undergroundV2_scaled
NowLocationRect = imports.undergroundV2_scaled_rect

walls = [

]

activator = [
    Activator.Activator(params.WIDTH / (params.WIDTH / 125), params.HEIGHT / (params.HEIGHT / 270), params.WIDTH / (params.WIDTH / 1795), params.HEIGHT / (params.HEIGHT / 810))
]

NPSSS = [

]

dialog_window = [

]

newXPos = params.WIDTH / (params.WIDTH / 990)
newYPos = params.HEIGHT / (params.HEIGHT / 470)